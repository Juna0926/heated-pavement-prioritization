# Data availability

This repository contains public-safe outputs from a municipal public-data competition project. Raw and intermediate datasets are **not redistributed** because the analysis combines multiple external public-data sources with distinct update cycles and usage terms.

A **public-safe technical excerpt** and selected figures are provided under `outputs/` and `assets/` to document the analytical workflow and results without redistributing the working datasets.
